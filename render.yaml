services:
  - type: web
    name: sfr-platform
    runtime: node
    plan: free
    branch: main
    autoDeployTrigger: commit
    rootDir: sfr-platform
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /
